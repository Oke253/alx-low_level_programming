#include <stdio.h>

/**
 * main - program entry point
 * Return:0 no error, non zero if erro
 */
int main(void)
{
	char i;

	for (i = '0'; i <= '0'; i++)
	{
		putchar(i);
	}
		putchar('\n');
	return (0);
}
